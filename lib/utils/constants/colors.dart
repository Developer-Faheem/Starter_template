import 'package:flutter/material.dart';

class SColors {
  SColors._();

//App Basic Colors
  static const Color primary = Color(0XFF486FF);
  static const Color secondary = Color(0XFF486FF);
  static const Color accent = Color(0XFF486FF);

//gradient Colors
  static const Gradient linearGradient = LinearGradient(
      begin: Alignment(0.0, 0.0),
      end: Alignment(0.707, -0.707),
      colors: [Colors.black, Colors.red, Colors.grey]);

//Text Colors

//background Colors
  //e.g => light ,dark

//background Container colors

//button Colors

//border Colors

//error and validation colors
  //error , success, warning , info

//neutral shades
}
